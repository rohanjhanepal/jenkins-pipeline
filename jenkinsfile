pipeline {
    agent any


    //tools {
    //    jdk 'JDK11'      
    //    maven 'Maven3'   
    //}
    //for build

    stages {
        stage('Build') {
            steps {
                echo 'Building with Maven...'
                //sh 'mvn clean package'
            }
        }

        stage('Unit and Integration Tests') {
            steps {
                echo 'Running unit and integration tests...'
                //sh 'mvn test'
            }
        }

        stage('Code Analysis') {
            steps {
                echo 'Running code quality analysis with SpotBugs...'
                //sh 'mvn com.github.spotbugs:spotbugs-maven-plugin:spotbugs'
            }
        }

        stage('Security Scan') {
            steps {
                echo 'Running OWASP Dependency-Check for vulnerabilities...'
                //sh 'mvn org.owasp:dependency-check-maven:check'
            }
        }

        stage('Deploy to Staging') {
            steps {
                echo 'Deploying JAR to staging environment...'
                //sh 'scp target/*.jar ec2-user@staging-server:/opt/todo-staging/' // Replace with your actual host/path
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                echo 'Running staging tests...'
                //sh 'curl -I http://staging-server:8080/todo' // Adjust endpoint
            }
        }

        stage('Deploy to Production') {
            steps {
                echo 'Deploying JAR to production environment...'
                //sh 'scp target/*.jar ec2-user@production-server:/opt/todo-prod/' // Replace with your actual host/path
            }
        }
    }
}
